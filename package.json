{
  "name": "house-rules",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "web": "npm run dev -w @house-rules/web",
    "sim": "tsx packages/sim/src/cli.ts",
    "bench": "tsx packages/sim/src/cli.ts bench",
    "gen:rules-gaps": "node scripts/gen-rules-gaps.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^5.5.4",
    "vitest": "^3.0.0"
  }
}
